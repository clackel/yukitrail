# REST API prefix. Keep /api/v1 when using the Vite development proxy.
VITE_API_BASE_URL=/api/v1

# Create browser-side credentials in the AMap console and restrict their domains.
VITE_AMAP_KEY=
VITE_AMAP_SECURITY_JS_CODE=

