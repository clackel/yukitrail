<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.yukitrail.api.auth.mapper.UserMapper">

    <resultMap id="userAccountMap" type="dev.yukitrail.api.auth.model.UserAccount">
        <id property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="nickname" column="nickname"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findById" resultMap="userAccountMap">
        SELECT id, email, password_hash, nickname, created_at, updated_at
        FROM users
        WHERE id = #{id}
    </select>

    <select id="findByEmail" resultMap="userAccountMap">
        SELECT id, email, password_hash, nickname, created_at, updated_at
        FROM users
        WHERE email = #{email}
    </select>

    <insert id="insert" parameterType="dev.yukitrail.api.auth.model.UserAccount"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (email, password_hash, nickname)
        VALUES (#{email}, #{passwordHash}, #{nickname})
    </insert>

</mapper>
